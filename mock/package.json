{
  "name": "mock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docez src build",
    "watch": "chokidar 'src/**/*' -c 'npm run build'",
    "serve": "sirv build --dev",
    "dev": "concurrently 'npm run serve' 'npm run watch'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^2.1.0",
    "concurrently": "^5.3.0",
    "docez": "file:..",
    "sirv-cli": "^1.0.8"
  }
}
