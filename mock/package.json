{
  "name": "mock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docez",
    "watch": "chokidar 'src/**/*' '../src/**/*' -c 'npm run build'",
    "serve": "sirv build --dev",
    "dev": "concurrently 'npm run serve' 'npm run watch'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^2.1.0",
    "concurrently": "^5.3.0",
    "sirv-cli": "^1.0.8"
  },
  "dependencies": {
    "docez": "file:.."
  }
}
